# Streamlit 1.52+ supports Python 3.14 and pulls Pillow with cp314 wheels.
# Older Streamlit (e.g. 1.39) pins pillow<11, which has no 3.14 wheel and
# fails building from source on Streamlit Community Cloud.
streamlit>=1.52,<2
pillow>=12.0.0,<13

langchain>=0.2.0,<0.3
langchain-openai>=0.1.0,<0.2
langchain-community>=0.2.0,<0.3
openai>=1.30.0,<2
nltk>=3.8.1,<4
PyPDF2>=3.0.1,<4
python-dotenv>=1.0.0,<2
